/**
 * Where the two images live.
 *
 * Their own module so a view can name a URL without importing 100KB of base64,
 * and so the passers-by counter, the route and the markup can never drift apart.
 */
export const PORTRAIT_PATH = "/portrait.png";
export const ICON_PATH = "/icon.png";
